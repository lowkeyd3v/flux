# --- App ---
APP_ENV=development

# --- Database (matches docker-compose.yml postgres service) ---
DATABASE_URL=postgresql+psycopg2://flux_user:flux_password@localhost:5432/flux_db

# --- CORS ---
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# --- Future service keys (leave blank until those milestones) ---
LLM_API_KEY=
WEATHER_API_KEY=
